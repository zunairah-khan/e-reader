#Open and parse an EPUB file extracting the raw text from each chapter
#Split that text into individual pages that fit the screen dimensions
#Draw those pages as images using the correct font and size
#Handle word wrapping so words never get cut off mid-word at the edge of the screen
#Draw the home screen showing your library with progress bars and battery
#Do all of this in a way you can test on your laptop by saving images as PNG files, then switch to sending them to the real display when the Pi arrives